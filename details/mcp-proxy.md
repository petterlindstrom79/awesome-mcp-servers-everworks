## Overview

MCP Proxy enables connection to MCP servers that run on SSE transport, and can expose stdio servers as an SSE server.

## Features

- Connect to MCP servers on SSE transport
- Expose stdio servers as SSE servers
- Transport layer bridging
- Server proxy capabilities