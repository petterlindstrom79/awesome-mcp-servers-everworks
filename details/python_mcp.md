## Overview

A Model Context Protocol server that allows LLMs to execute Python code locally on the user's machine.

## Features

- Run Python code snippets through MCP protocol
- Local execution environment
- Integration with MCP-compatible LLM clients