## Overview

A unified hub server that aggregates multiple MCP servers into a single SSE endpoint, reducing configuration complexity for LLM clients.

## Features

- Multiple MCP server consolidation
- Single SSE endpoint
- Simplified client configuration