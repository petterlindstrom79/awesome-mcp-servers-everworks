## Overview

A local router that acts as an aggregation proxy for MCP servers, providing a single access point for tools from multiple server implementations.

## Features

- MCP server aggregation
- Unified tool access
- Local routing proxy
- Multi-server management